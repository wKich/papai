services:
  papai:
    image: ${IMAGE:-ghcr.io/wkich/papai:latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/papai.db
    volumes:
      - papai-data:/data

volumes:
  papai-data:
