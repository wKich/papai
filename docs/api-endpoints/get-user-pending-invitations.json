{
  "name": "getUserPendingInvitations",
  "description": "Get all pending invitations for the current user",
  "method": "GET",
  "path": "/invitation/pending",
  "parameters": [],
  "requestBody": null,
  "responses": {
    "200": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "email", "workspaceId", "workspaceName", "inviterName", "expiresAt", "createdAt", "status"],
        "properties": {
          "id": { "type": "string" },
          "email": { "type": "string" },
          "workspaceId": { "type": "string" },
          "workspaceName": { "type": "string" },
          "inviterName": { "type": "string" },
          "expiresAt": { "type": "string" },
          "createdAt": { "type": "string" },
          "status": { "type": "string" }
        }
      }
    }
  },
  "enums": {},
  "tags": ["Invitations"]
}
