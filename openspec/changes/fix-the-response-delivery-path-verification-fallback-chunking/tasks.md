# Tasks: fix-the-response-delivery-path-verification-fallback-chunking

Ordered by dependency: the builder precedence change (group 1) feeds the trace observability and emission work (groups 2–3); adapter chunking (groups 4–6) is independent of the verifier workstream; docs and full verification close (group 7).

Atomicity contract: every implementation task is one complete red→green cycle — the reproducing tests and the implementation land in the same task, and the task is done only when its trailing **Verify** command passes, so no task ever leaves the tree red and no task is a test without its implementation (or an implementation without its test). Tasks 7.1–7.2 are the docs edit and the closing full-tree gate; they change no behavior and inherit an already-green tree. Loop verification is `bun run test:affected` (static heuristic — it cannot see `mock.module()` targets or DI seams, so trust the per-task Verify line over it); one full suite before finishing. All new `src/` branches must be asserted on both sides (mutation-ratchet note, design D5).

## 1. Verified-completion precedence (bugs 1–2)

- [x] 1.1 Empty/errored verifier means "skip verification" in `buildVerifiedCompletion` (`src/completion/verified-completion.ts`): widen `VerifiedCompletion` to `{ text, verdict, verifierOutcome: 'ok' | 'empty' | 'error' }`; on empty verifier text or a thrown verifier, log `warn` and deliver `turn.finalText` with verdict `unconfirmed` when it is non-empty and `finishReason !== 'tool-calls'` (outcome `empty` / `error`); on a `tool-calls` finish the preamble is never delivered — the activity-selected stub remains the deliverable; the verifier's non-empty text still wins with its derived verdict and outcome `ok`; no-model-text turns keep the stub matrix unchanged. Red→green additions in `tests/completion/verified-completion.test.ts`: empty-with-finalText and throw-with-finalText rows deliver `finalText`/`unconfirmed` with the matching outcome and a logged warn; `tool-calls`-finish row does not deliver the preamble (stub delivered, outcome recorded); exactly one verifier invocation on empty output (no retry); one-line rewrite of the `confirmed` row's whole-shape `toEqual` (~line 268) to gain `verifierOutcome: 'ok'`; existing stub rows stay green unchanged. One precedence matrix in one function — the empty/error/tool-calls/verifier-wins rows share the type widening and the suite, so they land as one cycle, not separate tasks. Verify: `bun run test tests/completion/verified-completion.test.ts` — new rows, the rewritten `confirmed` row, and every pre-existing row green.

## 2. Verifier outcome on the trace record (bug 2)

- [x] 2.1 Handle `llm:verifier` in `src/debug/llm-trace-collector.ts`: `LlmTrace` gains optional `turnId` (populated in `buildEndTrace` from the event; the pending already carries it — the field's only consumer is this handler, so both land together) and optional `verifierOutcome`; `handleLlmTraceEvent` scans `recentLlm` backwards for the newest trace with a matching `turnId` and sets `verifierOutcome` in place (ring holds references, so init payload, `/debug` polling, and transcript viewer see it); a no-match event (ring eviction) drops with a `debug` log and throws nothing. Red→green in `tests/debug/llm-trace-collector.test.ts`: `llm:end` then `llm:verifier` with the same `turnId` → the pushed trace ends with `verifierOutcome` set; unknown `turnId` → no match, nothing thrown; newest matching trace wins. Verify: `bun run test tests/debug/llm-trace-collector.test.ts` — collector suite green (collector-internal storage only; egress is 2.2, so the two stay separately verifiable tasks).
- [x] 2.2 Egress parity for `verifierOutcome`: `shapeLlmTrace` keeps the field for non-viewing admins (metadata like `finishReason`, no text) and `LlmTraceSchema` (`src/debug/schemas.ts`) gains the optional field so egress parse does not strip it. The two edits are one parity property — shaping retention without the schema (or vice versa) still strips the field at the boundary — so they are one task, not two. Red→green: shaped non-viewing trace keeps `verifierOutcome` in the `shapeLlmTrace` describe of `tests/debug/llm-trace-collector.test.ts`, plus a `LlmTraceSchema` round-trip row in `tests/debug/schemas.test.ts`. Confirm `APPROVED_EVENT_TYPES` (`src/analytics/subscriber.ts`) does not gain `llm:verifier` (analytics untouched by construction). Verify: `bun run test tests/debug/llm-trace-collector.test.ts tests/debug/schemas.test.ts` — both suites green and the analytics confirmation holds.

## 3. Verifier-outcome emission on the interactive path (bug 1 end-to-end)

- [x] 3.1 Emit `llm:verifier` from `sendLlmResponse` (`src/llm-orchestrator-send.ts`, wiring in `src/llm-orchestrator-support.ts`): `resolveFinalText` returns `{ text, verifierOutcome? }` instead of a bare string; the `Verification` argument widens to `{ verifier, history, turnId, chatUserId }`; `sendLlmResponse` emits `emitUser('llm:verifier', contextId, { chatUserId, outcome }, turnId)` whenever an outcome exists; `invokeWithLiveStatus` passes `invokeArgs.turnId` / `invokeArgs.chatUserId` into the verification struct it already builds. The signature widening has no observable behavior without the emission, so all seams land as one cycle. Red→green in `tests/llm-orchestrator-send.test.ts`: risky turn (tool failure, non-empty model text) + verifier returning `''` → the mock reply receives the model's own text and the emitted `llm:verifier` carries `empty`; verifier throw → event carries `error`; non-risky turn or no verifier → no event. The proactive path (`finalizeAndLog`) keeps its shape — no emission there (traces read "verifier not run"). Verify: `bun run test tests/llm-orchestrator-send.test.ts` — emission rows green, proactive-path rows unchanged.

## 4. Telegram chunked delivery (bug 3)

- [x] 4.1 Telegram splitter as a pure sibling module next to the adapter (`src/chat/telegram/format-chunking.ts`, per the Discord precedent — `reply-helpers.ts` sits at the 300-line `max-lines` cap): within-budget input returns one chunk; else cut at the last `\n\n` before the budget, else the last `\n`, else hard-cut at the budget; nudge the cut one code unit left when it would split a UTF-16 surrogate pair (astral characters never orphaned); trim leading boundary newlines; repeat on the remainder; the budget is an explicit parameter so first-chunk prefix reserve and reduced re-split budgets reuse the same function. Directly unit-tested like its Discord counterpart, so it is independently green before any consumer lands (4.2 and 4.3 both consume it — it is not coupled to either alone). Red→green direct unit rows in `tests/chat/telegram/format-chunking.test.ts` (mirroring `tests/chat/discord/format-chunking.test.ts`): paragraph/line/hard-cut boundary picks, surrogate-pair nudge, leading-newline trim, every chunk ≤ budget, within-limit single chunk. Verify: `bun run test tests/chat/telegram/format-chunking.test.ts` — the new suite green.
- [x] 4.2 Chunked `sendFormattedReply` (`src/chat/telegram/reply-helpers.ts`; the sequential send loop lives in the sibling module where the cap demands): split the markdown *before* `formatLlmOutput` and format each chunk independently (entity offsets are per-message); budget-check each chunk's *formatted* text against `telegramTraits.maxMessageLength` (4096) — formatting is not length-preserving — re-splitting an over-limit chunk from its markdown at a proportionally reduced budget until it fits (hard cut as floor); send chunks strictly in order with identical reply parameters (Discord `pLimit(1)` pattern — `no-await-in-loop` is enforced for `src/`); return the **first** chunk's `{ messageId, chatId }` so `lastReplyTarget`/`editReply` keep their single-ref shape; a failed chunk logs `warn` with `chatId` plus chunk index/count, remaining chunks are still attempted, and the first error is rethrown after the loop. The markdown-level split, the formatted-length re-split, the first-id return, and continue-then-rethrow are one function's contract, asserted by one suite — not separable tasks. Red→green in `tests/chat/telegram/reply-helpers.test.ts`: over-limit markdown delivered as ordered chunks each ≤ 4096 with paragraph/line boundaries and hard cut for unbroken text; within-limit formatted text → single message; middle-chunk failure → later chunks attempted, warn carries `chatId` + index/count, first error rethrown. Verify: `bun run test tests/chat/telegram/reply-helpers.test.ts` — chunking rows green, existing single-send rows unchanged.
- [x] 4.3 Chunked deferred `sendMessage` (`src/chat/telegram/index.ts`): chunk the markdown and format each chunk independently; the mention prefix (text + entity offset shift) lands only on the first chunk and its length counts against the first chunk's budget; every chunk carries the same `message_thread_id`. Red→green in `tests/chat/telegram/index.test.ts`: over-limit deferred delivery arrives as ordered in-limit chunks, first chunk prefixed, no prefix on followers, thread id preserved. Verify: `bun run test tests/chat/telegram/index.test.ts` — deferred-delivery rows green, existing rows unchanged.

## 5. Mattermost chunked delivery (bug 3)

- [x] 5.1 Chunked `formatted` in `createMattermostReplyFn` (`src/chat/mattermost/reply-helpers.ts`; splitter in a sibling module if the `max-lines` cap demands — the file is at 254 lines): split at the adapter trait 16383 (paragraph → line → hard cut, same algorithm as 4.1), send chunks in order via the existing `makePost` (same channel/thread), snapshot the **first** chunk's post id into `lastReplyTarget`; per-chunk failure logs `warn` with `channelId` plus chunk index/count, remaining chunks still attempted, first error rethrown. Red→green in `tests/chat/mattermost/reply-helpers.test.ts`: over-limit markdown → ordered posts each ≤ 16383 with boundary picks and hard cut; within-limit → single post; middle-chunk failure semantics with the `channelId`-bearing warn. Verify: `bun run test tests/chat/mattermost/reply-helpers.test.ts` — immediate-path chunking rows green, existing rows unchanged.
- [x] 5.2 Chunked `sendMattermostDeferredMessage` (`src/chat/mattermost/reply-helpers.ts`): chunk after resolving the mention prefix — prefix counted against the first chunk only; the dm path (no prefix) chunks under the same 16383 limit; every chunk carries the same `root_id`. A different function than 5.1 with its own rows in the shared suite — independently verifiable, so it stays a separate task. Red→green in `tests/chat/mattermost/reply-helpers.test.ts` (the helper's home suite): deferred group-personal over-limit send → ordered chunks, first prefixed; deferred dm over-limit send → ordered chunks, unprefixed. Verify: `bun run test tests/chat/mattermost/reply-helpers.test.ts` — deferred-path rows green (5.1's rows with them).

## 6. Kontur Talk chunked delivery (bug 3)

- [x] 6.1 Chunked `formatted` wrapper in `createKonturTalkReplyFn` (`src/chat/kontur-talk/reply-helpers.ts` — at 37 lines the splitter may live in-file): a markdown-format chunking variant of the internal `send`, leaving the plain `text` path on the unchunked `send` (`reply.text` Non-goal); sequential `/send_message` calls with the same `thread_id`, split at the adapter trait 4096; per-chunk failure logs `warn` with `roomId` plus chunk index/count, remaining chunks attempted, first error rethrown. Red→green in `tests/chat/kontur-talk/reply-helpers.test.ts`: over-limit markdown → ordered sends each ≤ 4096 with boundary picks and hard cut; plain `text` call stays one unchunked send; middle-chunk failure semantics. Verify: `bun run test tests/chat/kontur-talk/reply-helpers.test.ts` — chunking and plain-path rows green.
- [ ] 6.2 Chunked deferred `sendMessage` (`src/chat/kontur-talk/index.ts`): chunk the deferred markdown post at 4096 into sequential `/send_message` calls with the same `thread_id`. Red→green in `tests/chat/kontur-talk/index.test.ts`: over-limit deferred group delivery arrives as ordered in-limit chunks with the thread id preserved. Verify: `bun run test tests/chat/kontur-talk/index.test.ts` — deferred-delivery rows green, existing rows unchanged.

## 7. Docs and full verification

- [ ] 7.1 Update the `verified-completion` bullet in `docs/architecture/behaviors.md` (~line 82) for the new semantics: empty/errored verifier delivers the turn's model text (skip, never re-ask; step-capped preamble falls through to the stub), the stub is a true last resort, the interactive turn's trace record carries `verifierOutcome`, and over-limit replies deliver as ordered chunks on Telegram/Mattermost/Kontur Talk with continue-then-rethrow failure semantics. Docs only — no code path touched. Verify: `bun run format:check` passes (markdown formatting) and the doc renders the four semantics above.
- [ ] 7.2 Full-tree verification gate over the already-green tasks above (no implementation lives here, and nothing below depends on it): run `bun run test` (full suite green) and the `bun check:full` pipeline (lint incl. typecheck, knip, format, duplicates); run `bun run test:mutate:changed` for the touched `src/` files and confirm both sides of every new branch are covered (outcome precedence, preamble guard, split boundaries, first-chunk reserve, continue-then-rethrow) per the design's mutation-ratchet note. Verify: all three commands exit green.
