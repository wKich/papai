# afk-runner scenario fixtures

Scenario logs covering event shapes the real-run hoard (`../real/`) lacks. Extracted
from the inline event sequences crafted in `tests/sdd-runner/` test files — values are
taken from those sequences, not invented. Fold all of them with `legacy-fold` (asserted
by `inventory.test.ts` in this directory).

| fixture                                     | covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | extracted from                                                                                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `s-depth-calm-stop-resume.ndjson`           | S depth profile (no real S run exists); calm-stop boundary (a calm stop leaves no event — the log simply ends at a round boundary); `resume` `session-continuation` at review                                                                                                                                                                                                                                                                                                                           | `tests/sdd-runner/stop-controller.test.ts` (`makeReviewFixture` events), `tests/sdd-runner/resume-decision.test.ts` (session ledger line `ses_x`)                         |
| `resume-artifact-skip-gate.ndjson`          | `resume` `artifact-skip` path (real logs only carry `stage-rebuild` / `session-continuation`); early gate pending at the resume point                                                                                                                                                                                                                                                                                                                                                                   | `tests/sdd-runner/resume-decision.test.ts` (artifact-skip decisions), gate events per `tests/sdd-runner/replay.test.ts`                                                   |
| `steer-extend-round.ndjson`                 | steer `extend` consumption: cap-hit convergence, `auto_decision` `R2`/`extend`, then a raised-cap round (steer directives are file-based — the log only records their effect)                                                                                                                                                                                                                                                                                                                           | `sdd-runner/src/gate-settle.ts` `autoExtendRound` emit shape, `sdd-runner/src/review-loop.ts` `round_open` with `applySteerAtBoundary` effective cap                      |
| `children-plan-synthetic.ndjson`            | **synthetic** — `plan` / `child_spawned` / `child_done` have no runtime producer today (schema + fold only; see `sdd-runner/src/event-schemas.ts` and the children fold in `tests/sdd-runner/replay.test.ts`)                                                                                                                                                                                                                                                                                           | `tests/sdd-runner/replay.test.ts` (children fold 3.3 sequences, digests `aaaa`/`bbbb`)                                                                                    |
| `veto-revision-synthetic.ndjson`            | **synthetic** — the veto settle shape (C4 D8): zero veto events exist in the corpus; `gate answered` with explicit `outcome: "veto"` followed by the `stage_enter(draft)` re-entry mover                                                                                                                                                                                                                                                                                                                | `afk-runner/src/work/gate-settle.ts` settle emit shapes over the `steer-extend-round` presentation prefix                                                                 |
| `s-final-tail-synthetic.ndjson`             | **synthetic** — the depth-S final tail (C5): decompose bracket hosting the final-gate presentation (`stage_enter(gate)` from decompose, presented, ladder, exit-after-presented bracket close); no atomicity events. Red seed for the `decompose → gate` kernel edge                                                                                                                                                                                                                                    | C5 tail-on-graph design D2/D4 ordering; corpus completed-tail choreography (`2026-08-21T19-44-19-770Z-2f6e644a` seq 882–885) re-hosted on the S path                      |
| `same-round-resume-honest-synthetic.ndjson` | **synthetic** — the honest same-round resume (log-fidelity): round 1 opened once by the dying process, the resume event (`session-continuation`) from the new invocation, the self-loop re-entry, and the re-run round's work-shaped facts (findings, convergence, `round_close`) with no second `round_open`; the S tail completes. Kernel accounting counts `resume` as tolerated; the legacy fold replays it as a strict no-op (asserted in `inventory.test.ts`)                                     | the post-owedness shape of the live kill/resume drill (the C7 seq 195/202 pair) + the `s-depth-calm-stop-resume` resume line over the `under-budget-retry-synthetic` tail |
| `extend-at-final-cycle-synthetic.ndjson`    | **synthetic** — extend-at-final full cycle (C5 D3): final gate presented (v1, parked), extend settles answered-first (no completion), tail re-runs as fresh brackets, re-presentation at v2, approve settles exit→answered → completed                                                                                                                                                                                                                                                                  | C5 outcome-ordered settle shapes over the M tail choreography                                                                                                             |
| `abort-at-final-synthetic.ndjson`           | **synthetic** — abort at a final gate (C5 D3): the answered outcome=abort event alone reaches the aborted final (the aborted edge ignores the stage map)                                                                                                                                                                                                                                                                                                                                                | C5 settle seam abort branch                                                                                                                                               |
| `veto-at-final-cycle-synthetic.ndjson`      | **synthetic** — veto-at-final full cycle (C5 D3): veto settles answered→exit→draft mover, revision round (draft bracket + review round 2), tail re-runs, re-presentation at v2, approve completes                                                                                                                                                                                                                                                                                                       | C5 settle seam veto branch + `gate-settle.ts` mover shapes                                                                                                                |
| `tail-crash-resume-synthetic.ndjson`        | **synthetic** — W3a crash window (C5 D5): the log ends after `stage_enter(gate)` but before the presented event — gate stage active, gate record null (atomicity already closed by the gate entry's closeThenActivate)                                                                                                                                                                                                                                                                                  | C5 owed-presentation resume crash shape                                                                                                                                   |
| `tail-crash-resume-healed-synthetic.ndjson` | **synthetic** — the healed form of the W3a crash (C5 D5): owed `presented` at the file-scan version + ladder re-run — parked gate-pending                                                                                                                                                                                                                                                                                                                                                               | C5 owed-presentation recovery append order                                                                                                                                |
| `escalation-approve-cycle-synthetic.ndjson` | **synthetic** — the escalation-approve full cycle (C6 D4): review fails twice (first failure under budget re-runs the bracket), the budget-exhausted escalation gate presents interstitially from review's position (the failed stage stays active, no `stage_enter(gate)`), the ladder logs rule none under ceiling, approve settles answered mode `escalation` + the exit+enter retry mover (fresh bracket, ledger cleared), the round converges, and the S tail completes through a final gate at v2 | C6 escalation face shapes — red seed: the awaiting→review enter edge                                                                                                      |
| `escalation-extend-cycle-synthetic.ndjson`  | **synthetic** — the escalation-extend full cycle (C6 D4): same presentation prefix; extend settles answered→exit→enter (identical mover shape — both retry outcomes re-enter through a fresh bracket), the round re-runs, the S tail completes at v2                                                                                                                                                                                                                                                    | C6 settle seam extend branch over the approve-cycle prefix                                                                                                                |
| `escalation-abort-synthetic.ndjson`         | **synthetic** — the escalation-abort terminal (C6 D4/D8): the answered outcome=abort event alone reaches the aborted final — the `failed` memo shape (abort settled at an escalation gate); the failure ledger rides the terminal context                                                                                                                                                                                                                                                               | C6 settle seam abort branch + memoFieldsOf failed derivation                                                                                                              |
| `precondition-escalation-synthetic.ndjson`  | **synthetic** — precondition-immediate (C6 D1/D3): ONE `stage_failed {kind: precondition}` at atomicity (tasks.md missing) escalates on the spot — the presented event moves atomicity's position interstitially, approve retries, the M tail completes at v2                                                                                                                                                                                                                                           | C6 atomicity presented edge + awaiting→atomicity mover + escalationOwed precondition branch                                                                               |
| `under-budget-retry-synthetic.ndjson`       | **synthetic** — the under-budget retry (C6 D3) as **tolerated history**: review fails ONCE, the bracket re-runs in place (no gate, no re-enter — exactly one review enter/exit), and the double `round_open(1)` the pre-log-fidelity emission produced stays load-bearing — both folds are last-write-wins over it; new logs emit exactly one. The run completes with no escalation ever presented                                                                                                      | C6 loop catch under-budget branch + the live-run drill's log shape (the double is the frozen seq shape log-fidelity's owedness invariant removed)                         |
| `armed-approval-synthetic.ndjson`           | **synthetic** — the armed final-approve ordering (U3 D3): presented final v1, settle appends the presenting exit + gate exit, the implement mover, THEN the answer — the log ends implement-active with the walk not yet started (the post-mover window)                                                                                                                                                                                                                                                | the PRELUDE of `tests/afk-runner/work/gate-settle-release.test.ts` (the slice-3/6 settle emission shapes)                                                                 |
| `task-walk-synthetic.ndjson`                | **synthetic** — the sequential walk mid-flight (U3 D4): item 1 done, item 2 started-not-done, implement active, verify never entered — the mid-implement kill window a resume continues per-task                                                                                                                                                                                                                                                                                                        | the walk choreography pinned by `tests/afk-runner/work/implement.test.ts` over the same prelude                                                                           |
| `red-verify-fix-loop-synthetic.ndjson`      | **synthetic** — the red boundary loop (U3 D5): walk done, verify red routes back into implement (a normal outcome — no `stage_failed`), the fix re-starts the culprit item (attempts rise to 2) and answers the verify log artifact in place, verify re-runs green, release parks presented v2                                                                                                                                                                                                          | `tests/afk-runner/work/verify.test.ts` red-boundary drill emission shapes over the walk prelude                                                                           |
| `attempt-bound-exhaustion-synthetic.ndjson` | **synthetic** — attempt-bound exhaustion (U3 D4/C6): item 1 fails its two red checks, the `TASK_FIX_ATTEMPTS` bound halts the bracket twice (two `stage_failed exhausted`), and the escalation gate presents interstitially from implement's position — parked awaiting the operator                                                                                                                                                                                                                    | `implement.ts` StageHaltError branch + the C6 interstitial escalation shapes (compare `escalation-approve-cycle-synthetic`)                                               |
| `release-approval-synthetic.ndjson`         | **synthetic** — the release approve settle (U3 D7): one-item walk, verify green, release presents v2, settle appends exit-then-answer — completed with no implement mover, tasks `{1: done ×1}`                                                                                                                                                                                                                                                                                                         | `gate-settle-release.test.ts` approve settle shapes over the PRELUDE                                                                                                      |
| `release-veto-synthetic.ndjson`             | **synthetic** — the release veto cycle (U3 D7): veto answers first, the mover re-enters implement, the redirect is answered in the `release-veto.md` sidecar (artifact — not a log event), the culprit re-walks (attempts 2), verify re-runs, release re-presents v3, approve completes                                                                                                                                                                                                                 | `gate-settle-release.test.ts` veto settle + the veto-then-approve loop drill emission shapes                                                                              |
| `execution-crash-windows-synthetic.ndjson`  | **synthetic** — the reversed armed-approve window (U3 D3): the mover landed but the answer never did — implement active under a presented-unanswered final v1; `owedAnswerOf` heals exactly the missing approve on resume                                                                                                                                                                                                                                                                               | `run-recovery.ts` `owedAnswerOf` row over the approval prelude minus its last event                                                                                       |
