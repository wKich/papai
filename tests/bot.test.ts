import { describe, test, expect } from 'bun:test'

// Authorization tests for checkAuthorizationExtended live in bot-auth.test.ts
describe('bot', () => {
  test.todo('setupBot registers commands and message handler', () => {
    expect(true).toBe(true)
  })
})
